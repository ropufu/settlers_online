
CC = clang++

CFLAGS = -std=c++14 -stdlib=libc++ -Wall

PATHINC = -I./../../aftermath -I./..

PATHLIB =

LDFLAGS =

.VPATH: .

test_win32.bin: $(patsubst %.cpp, %.o, $(wildcard *.cpp))
	$(CC) $^ -o $@ $(PATHLIB) $(LDFLAGS) $(LDLIBS)
	rm -rf *.o *.d

%.o: %.cpp
	$(CC) $< $(CFLAGS) -c -MD $(PATHINC)

include $(wildcard *.d)

.PHONY: clean

clean:
	rm -rf *.o *.d

